/*
Create a function that takes a person’s name as a parameter, and returns a greeting string like:
"Hello, Maz!"
*/

const greet = (name) => {
    return "Hello, " + name + "!"
}

const result = greet("Maz");

console.log(result)

